
pipeline {
    agent any
	
    stages {
        stage('Test Parameter') {
            steps {
                script {
				    echo "ABC=${ABC}"
					echo "Type=${Type}"
					SHELL_PATH="${env.WORKSPACE}/shell/testShell.sh"
					echo "SHELL_PATH=${SHELL_PATH}"
					sh "${SHELL_PATH}" 
                }
            }
        }
    }
}
